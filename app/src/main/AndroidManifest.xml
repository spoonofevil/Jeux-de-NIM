<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="spoony.jeuxdenim">

    <application
        android:allowBackup="true"
        android:icon="@drawable/nimesroundicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/nimesroundicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.JeuxDeNim">
        <activity android:name=".activity.TimberActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".activity.Pebble1bot"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activity.Pebble1Activity"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.GameListActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>